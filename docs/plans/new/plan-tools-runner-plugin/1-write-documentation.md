# Step 1: Write documentation

Draft the user-facing and developer-facing documentation for the `tools-runner` plugin **before any code is written**, so the user can read the docs and understand exactly what they will be getting. The docs are subsequently revised in the final step (`15-update-documentation.md`) to reflect any deviations made during implementation.

This step delivers four markdown files in their first-pass form. The content is described in detail in plan section 17: follow the structure pinned there. Do not add fields, sections, or examples not specified by the plan.

## Files to create

### 1.1. `./README.md`

Tight pitch + install + ONE example + links out. Per plan 17.1:

- Heading and 1–2 sentence pitch.
- Installation: clone, `bun install`, `bun run bundle`, then either `claude --plugin-dir ./plugin` for a single project or register the `Stop` hook in `~/.claude/settings.json`.
- A single minimal yaml example with one trigger running `bun run test` on TS changes, dropped at `<project>/.claude/tools-runner.yaml`. Two-line description of what will happen.
- "Learn more" section linking to `docs/CONFIGURATION.md`, `docs/HOW_IT_WORKS.md`, and `docs/DEVELOPMENT.md`.
- No exhaustive field reference, no template-token table, no layering explanation here: those live in `docs/CONFIGURATION.md`.

### 1.2. `./docs/CONFIGURATION.md`

The user-facing reference for writing trigger configs. Per plan 17.2, include every section listed there:

- **Where configs live**: `~/.claude/tools-runner.yaml` (home, optional) and `<project>/.claude/tools-runner.yaml` (project, optional).
- **Layering**: home triggers listed first, project triggers second; both contribute. No name-based override; same `(run, cwd)` in both files share `commandKey` (last-write-wins on cooldown state).
- **Layering diagram** (the ASCII block from plan 17.2).
- **Auto-reload diagram** (the sequence block from plan 17.2).
- **Schema reference**: table or bullet list of every YAML field: `triggers[]`, `triggers[].paths[]`, `triggers[].commands[]`, `triggers[].commands[].run` (required), `.cooldown` (required, duration string like `"30s"`/`"5m"`/`"1h"`; bare digits = seconds; `"0"`/`"0s"` disables), `.cwd` (optional, defaults to `"${{project}}"`), `.timeout` (optional, duration string, defaults to `"5m"`).
- **Duration format subsection**: pin the format with examples and supported units (`s`, `m`, `h`); call out that bare YAML numbers, decimals, negatives, and other units (`ms`, `d`) are rejected.
- **Variables**: `${{repo}}`, `${{project}}`, `${{file_path}}`, `${{file_dir}}` with their availability scope. Pre-match (`${{repo}}`, `${{project}}`) work in `paths`, `run`, and `cwd`; post-match (`${{file_path}}`, `${{file_dir}}`) work in `run` and `cwd` only. Per-file variables cause one invocation per unique expansion. `${{repo}}`/`${{project}}` in a `paths` pattern have their absolute prefix stripped after expansion so the result stays repo-relative.
- **Worked examples**: the four examples enumerated in plan 17.2 (project tests, per-file linter, per-package build, home + project layered).
- **State file note**: `.claude/tools-runner-state.yaml` is auto-created and gitignored; delete to reset.
- **Troubleshooting**: what stdout/stderr look like, how to verify a trigger is matching (`git status --porcelain`), how to bypass cooldown for testing (`cooldown: "0s"`).

### 1.3. `./docs/HOW_IT_WORKS.md`

The internals reference (architecture, not config syntax). Per plan 17.3 and the Issue 22 resolution:

- High-level architecture diagram showing: Claude Code → Stop hook → load layers (home + project YAML) → git status → glob match → hash → cooldown gate → spawn commands → state file.
- Section per concern: layered config loading, changed-file collection (staged + unstaged, deletions skipped, renames take destination), glob matching against repo-relative paths, hash computation (SHA-256 over sorted `path\0hash\n` lines, with per-file `mtimeMs` + size cache), command-run state keyed by `sha256(expandedRun + "\0" + expandedCwd)`, cooldown semantics (the four-branch decision table from plan section 12.1), parallel execution, state file shape.
- A sequence diagram of three consecutive Stop events: first run executes; second run inside cooldown skips and resets cooldown when no change; third run after cooldown with same hash skips without resetting.
- **Literal state-file YAML example**: include the fenced YAML block from plan 17.3 verbatim. It is the canonical reference for smoke test scenario 7, the implementation of `saveState`, and any code that round-trips state through `yaml`. Use camelCase keys (`fileHashes`, `commandRuns`, `commandKey`, `expandedRun`, `expandedCwd`, `lastRunAt`, `lastFilesHash`, `matchedFiles`, `mtimeMs`).
- For YAML syntax, link out to `docs/CONFIGURATION.md` rather than duplicating.

### 1.4. `./docs/DEVELOPMENT.md`

Per plan 17.4 and the Issue 24 resolution:

- Prerequisites: Bun.
- Clone and bootstrap: `git clone`, `cd claude-tools-runner`, `bun install`, `bun run bundle`.
- Running it during development (`claude --plugin-dir ./plugin` or via `~/.claude/settings.json`).
- Running tests and type-check: `bun run compile` (TypeScript type-check via `tsc --noEmit`), `bun run test` (Jest unit tests), `bun run smoke` (end-to-end behavioral scenarios), `bun run hook-smoke` (bundle-integrity checks). `bun run test:all` runs unit + hook-smoke + smoke as a single gate.
- **Note on missing `bun`** (Issue 15): if `bun` is not on `$PATH` the Stop hook silently fails to start; install Bun or remove the hook line from `~/.claude/settings.json`. For user-configured commands that use `bun`, a missing `bun` causes `sh -c` to exit 127: handled identically to any other non-zero exit.
- (Section dropped from DEVELOPMENT.md by user request: see Summary deviation note.)
- Bundling and publishing: `bun run bundle`; what files end up in `plugin/dist/`.
- Troubleshooting: state file location, how to reset, observe stdout/stderr; the audit log captures the full event timeline.

## Verification

- All four files exist at the paths above.
- Every relative link in `README.md` resolves to an existing file (`docs/CONFIGURATION.md`, `docs/HOW_IT_WORKS.md`, `docs/DEVELOPMENT.md`).
- All fenced code blocks in every doc are properly closed (no unmatched ` ``` `).
- Every YAML example in any doc parses successfully (use `bun -e` with the `yaml` package or a `python -c` snippet).
- The state-file YAML example in `docs/HOW_IT_WORKS.md` uses the canonical camelCase keys defined in plan section 6.

Run all tests and confirm they pass before marking this step complete. (For this step, "all tests" means the doc-integrity checks above plus the existing test suite if any has been written; no source code is added, so the unit/smoke suites do not yet exist.)

## Summary

Drafted four documentation files in their first-pass form, before any source code. All four follow the structure pinned in plan section 17.

Files created:

- `./README.md`: pitch, install (clone + `bun install` + `bun run bundle`, then either `claude --plugin-dir ./plugin` or a `Stop` hook entry in `~/.claude/settings.json`), single yaml example for `bun run test` on `src/**/*.ts`, "Learn more" links to the three doc files.
- `./docs/CONFIGURATION.md`: where configs live, layering with the ASCII diagram, auto-reload sequence diagram, full schema table (every field with type/required/default/notes), variables table with availability scope (`paths`/`run`/`cwd`) and per-file grouping rules, four worked examples (project tests, per-file linter via `${{file_path}}`, per-package build via `${{file_dir}}`, home + project layered), state-file note + reset instructions, troubleshooting subsection covering log-line shapes, `git status --porcelain` verification, `cooldown: "0s"` for testing, missing `bun` (Issue 15).
- `./docs/HOW_IT_WORKS.md`: ASCII architecture diagram (Claude → hook → load layers → git → match → hash → gate → spawn → state), section per concern (layered loading, changed-file collection, glob matching, hash computation with sentinels, CompiledCommand grouping with the four-row table, command-run state shape, the four-branch cooldown table, parallel execution with global timeout), three-Stop-event sequence example, literal state-file YAML block (verbatim from plan 17.3) using canonical camelCase keys (`fileHashes`, `mtimeMs`, `commandRuns`, `commandKey`, `expandedRun`, `expandedCwd`, `lastRunAt`, `lastFilesHash`, `matchedFiles`).
- `./docs/DEVELOPMENT.md`: prerequisites (Bun), clone + bootstrap, dev usage (plugin-dir vs source mode), test/compile/smoke commands table including `bun run compile` (Issue 24), missing-`bun` note (Issue 15), adding-a-template-token recipe, bundling/publishing, troubleshooting (state file location, why a command might keep re-running or never run).

Verification:

- All four files exist at the documented paths.
- Every relative link in `README.md` (`docs/CONFIGURATION.md`, `docs/HOW_IT_WORKS.md`, `docs/DEVELOPMENT.md`) resolves.
- All fenced code blocks are properly closed (8/16/8/8 fence markers, all even).
- All 7 YAML examples across the four docs parse cleanly via `python3 -c "import yaml; yaml.safe_load(...)"` (the project does not yet have its own `yaml` package: that arrives in step 2).
- The state-file YAML example in `docs/HOW_IT_WORKS.md` uses the canonical camelCase keys defined in plan section 6.

Decisions and divergences:

- The plan's CONFIGURATION.md spec asked for "where each [token] is allowed". Documented this as the per-file grouping rule (which dimension forces per-file vs per-dir vs single emission), since both `run` and `cwd` accept all four tokens: there is no token that's allowed in only one. Matches the rule pinned in plan section 11.1 and Issue 17.
- Added explicit notes about shell-quoting behavior of `${{file_path}}` / `${{file_dir}}` in `run` (matching the Issue 4 resolution) so users understand why no manual quoting is needed. Not in the plan's outline, but follows naturally from the schema reference.
- Added a brief "File-size cap" subsection to CONFIGURATION.md (Issue 26 resolution). Originally documented the cap as the `TOOLS_RUNNER_HASH_FILE_MAX_BYTES` env var; later revised to a hardcoded 64 MiB constant; finally **dropped entirely**: user confirmed source-tree files never approach the danger zone, so no cap is needed. The `<missing>` sentinel for ENOENT remains. The "File-size cap" subsection in CONFIGURATION.md was removed and the corresponding mention in HOW_IT_WORKS.md was simplified to just describe `<missing>`.
- The Stop hook command in README's global-install snippet originally used `/absolute/path/to/tools-runner/plugin/dist/stop-hook.js` as a placeholder. **Subsequently revised** (see deviation below): the global-install snippet was removed entirely.

Deviation from plan 17.1: README install section:

- Plan 17.1 specifies the README install path as: clone, `bun install`, `bun run bundle`, then either `claude --plugin-dir ./plugin` or register the `Stop` hook in `~/.claude/settings.json`. That is a *developer* install path.
- A *user-facing* marketplace install path is preferred instead: `/plugin marketplace add ashleydavis/<plugin>` + `/plugin install <plugin>@<plugin>`, with a stable release section and a `dev` branch pre-release section. The clone+bundle path lives in `docs/DEVELOPMENT.md`.
- Resolution: the README uses the marketplace-install pattern: stable release block (`/plugin marketplace add ashleydavis/tools-runner`, `/plugin install tools-runner@tools-runner`) and a dev branch block. The clone+bundle path is referenced via "see docs/DEVELOPMENT.md" and is documented in full there. The plan's outline for README install was followed up to a point, then revised to match this pattern.

Subsequent revisions during this step (in response to user review):

- **README pitch revised** to match the description used in `marketplace.json` and `plugin.json` (plan section 2.1/2.2): *"A smart tool runner: runs your tests only when files have changed since the last time they were run."* The original pitch was longer and more technical; this version aligns with the manifest descriptions.
- **README link descriptions** rewritten in plain "what is this doc for" language: e.g. "How to write your `tools-runner.yaml` configuration" rather than "full schema, layering, template tokens, and worked examples".
- **README example expanded** to include `timeout` so all three required-or-commonly-tuned fields (`run`, `cooldown`, `timeout`) appear together. `cwd` was deliberately left out since it defaults to `${{project}}` and would add noise to a minimal example.
- **Schema rename: `globs` → `paths`.** The user-facing YAML field is now `paths` instead of `globs`. `paths` reads more naturally than the technical jargon `globs` while still being accurate (the values are picomatch patterns matched against repo-relative paths). Propagated through README, CONFIGURATION.md, HOW_IT_WORKS.md, the main plan, and every step file (`2-project-skeleton.md`, `3-config-loader.md`, `5-state-file.md`, `7-glob-matcher.md`, `10-compile-commands.md`, `14-smoke-tests.md`, `1-write-documentation.md`). The TypeScript `Trigger` interface field was also renamed (`globs: string[]` → `paths: string[]`), as was the `matchFiles` parameter (`globs` → `paths`). "Glob" remains in prose where it describes the *kind of value* the field holds (e.g. "picomatch glob patterns"). The step-file name `7-glob-matcher.md` was kept (the module is still a glob-matching utility: the rename only affects the user-facing YAML field name).
- **No env-var configuration; no global hook timeout at all.** Originally the plan had three env vars (`TOOLS_RUNNER_GLOBAL_TIMEOUT`, `TOOLS_RUNNER_HASH_FILE_MAX_BYTES`, `TOOLS_RUNNER_STATE_TTL_DAYS`). The user directed that nothing should be configured via env vars. Resolution went through two iterations:
  1. First iteration: only `globalTimeout` was exposed, as `settings.timeout` (top-level YAML field, duration string, default `"10m"`, project-wins layering); the state TTL became a hardcoded 30-day constant; the hash size cap was dropped (no cap).
  2. Second iteration: the user pointed out `settings.timeout` was redundant given per-command `timeout` already covers hung commands. Resolution: dropped `settings.timeout` entirely. With it gone, the entire `settings` block has no fields, so the top-level `settings` key was removed from the schema. The `Settings` interface, `Config.settings` field, `ITriggerLayer.settings()` method, `TriggerRegistry.mergedSettings()` method, and the abort-timer / `activeChildren` / `hook_timeout` audit-log entry all disappeared with it.

  Final state: the YAML has exactly one top-level key (`triggers`). Per-command `timeout` (default `"5m"`) is the only kill timer. The state TTL (30 days) and the absence of a hash size cap are hardcoded. Propagated through README, CONFIGURATION.md (schema simplified to one top-level key), HOW_IT_WORKS.md (parallel-execution section says "no global cap"), the main plan (types section 3, loader section 4.2, registry section 5, stop-hook section 14.1, log-line catalog drop, Hook behavior table drop, Issue 12 marked moot, Issue 13 trimmed), step files (2 types, 3 loader, 4 registry, 12 runner, 13 stop-hook, 14 smoke, 15 audit-log, 16 update-doc), and DEVELOPMENT.md troubleshooting. Final audit: zero `TOOLS_RUNNER_*` env-var references, zero `settings.timeout` references, zero `mergedSettings` / `activeChildren` / `hook_timeout` references anywhere in docs or plan.

- **Schema change: `cooldown` and `timeout` are now duration strings.** The fields were previously typed as numeric seconds in the YAML. They are now always strings of the form `<digits>` (interpreted as seconds when no unit suffix is given) or `<digits><unit>` where unit is one of `s`, `m`, `h`. Examples: `"30"`, `"30s"`, `"5m"`, `"1h"`, `"0"`, `"0s"`. Bare YAML numbers (`cooldown: 30`) are rejected: quote the value or add a unit suffix. Decimals (`"1.5s"`), negatives (`"-5s"`), and other units (`"500ms"`, `"7d"`) are also rejected. The TypeScript `CommandConfig` interface is unchanged (`cooldown: number`, `timeout?: number`, both integer seconds): parsing happens once in `loadConfigFile` via a new pure helper `parseDuration(input, fieldName)` in `src/duration.ts`. Propagated through README, CONFIGURATION.md (new "Duration format" subsection), HOW_IT_WORKS.md (note that the gate's `command.cooldown` is post-parse seconds), the main plan (sections 3, 4, 17 + new section 4.1 for `parseDuration`), the dogfood YAML, every worked example, and step files (`2-project-skeleton.md` types/dogfood, `3-config-loader.md` adds the duration helper + `src/test/duration.test.ts`, `1-write-documentation.md`/`14-smoke-tests.md`/`15-update-documentation.md` reference `cooldown: "0s"` for the cooldown-bypass pattern). A new unit-test file `src/test/duration.test.ts` was added to the plan's Unit Tests section.

- **Variables (was: "placeholders") and `paths` scope.** The user pushed back on the term "placeholders" as too jargony and asked for a better name; the section was renamed to **Variables**. Separately, the user noted the variables were originally introduced *specifically* so they could be used in `paths`. Implementation reflects that:
  - `${{repo}}` and `${{project}}` are pre-match: available in `paths`, `run`, and `cwd`.
  - `${{file_path}}` and `${{file_dir}}` are post-match (populated from matched files): available in `run` and `cwd` only; using them in `paths` would be circular and is rejected at validation.
  - `matchFiles` now takes a `TemplateContext` argument and runs each `paths` pattern through a new `expandPathPattern(pattern, ctx)` helper before glob matching. `expandPathPattern` substitutes `${{repo}}`/`${{project}}` and strips the absolute prefix afterwards so the pattern stays repo-relative; `!`-prefix negation is preserved across the strip; `${{file_path}}`/`${{file_dir}}` throw.
  - Propagated through CONFIGURATION.md ("Variables" section with availability table), HOW_IT_WORKS.md (Glob matching section now describes pre-match expansion), main plan (sections 8 matchFiles, 10 template, 11 compileCommands, plus tests in 15.4 and 15.6), and step files (7 glob-matcher with new `ctx` parameter and tests, 9 template-expansion adds `expandPathPattern` + tests, 10 prepare-commands threads `ctx` into `matchFiles`).
  - The previous deviation note in this step's Summary said variables in `paths` were not supported. That was wrong; it has been retracted via this entry.

- **Spawn switched from `Bun.spawn` to Node's `child_process.spawn`; per-command log files added.** Originally the plan used `Bun.spawn` for user commands, git invocations, and test fixtures. The user directed that the plugin must use Node-compatible async spawn instead, and that each command's stdout+stderr should be captured to a per-command log file under `.claude/`. Resolution: every spawn now uses `child_process.spawn` from `node:child_process` with `stdio: ["ignore", "pipe", "pipe"]`, wrapped in a Promise around `close`/`error` events. The `Spawner` DI seam was reshaped to match Node's `ChildProcess` subset. ENOENT detection goes through Node's `error` event (Bun's was a synchronous throw). Per-command logs sit at `<projectDir>/.claude/tools-runner-log/YYYY-MM/DD/HH/MM-SS-<ms>-<commandKey-first8>.log`: the per-hour `HH/` subdirectory is a sibling of the audit log's `HH.json`/`HH.log` files. Audit log entries `command_started` and `command_result` gain a `logFile` field linking each event to its full output. Retention unified at 1 month for the entire `tools-runner-log/` tree (was 3 months for audit; reduced for unified cleanup since both live together). Propagated through main plan (Implementation conventions, Issues 9 + 16, sections 7.1, 10.1, 13.1, log-line catalog), step files (6 git-collector, 12 runner full rewrite, 13 stop-hook test fixture, 15 audit-log path helpers + entry schemas + cleanup), HOW_IT_WORKS.md (architecture diagram + parallel-execution section), CONFIGURATION.md (quoting note rephrased), and step 9 template expansion (drops `Bun.spawn` from `cwd` references). `Bun.spawn` is explicitly banned in the Implementation conventions section.

- **Per-package grouping (`group_by` field + `${{group_dir}}` variable).** The original plan only had per-file (`${{file_path}}`/`${{file_name}}`) and per-directory (`${{file_dir}}`) granularity. The user pointed out that a real per-package use case (run a build once per monorepo package) didn't fit either: `${{file_dir}}` is the file's immediate parent, not the package root. Resolution: added a trigger-level `group_by` glob field. When set, the runner computes per-file `groupDir = findGroupDir(file.path, trigger.group_by)` (the first segment-prefix that matches `group_by`) and `${{group_dir}}` expands to that group's absolute path. The grouping rule gains a new tier between per-directory and per-trigger: if `run`/`cwd` uses `${{group_dir}}` (and no finer-granularity vars), one emit per unique group. Files whose path doesn't match `group_by` are dropped from the trigger's matched-files set with a stderr warning. Using `${{group_dir}}` in a trigger without `group_by` is a prepare-time validation error. Propagated through main plan (Trigger interface, section 4.2 loadConfigFile validation, section 8 matcher rejection list, section 11 compileCommands grouping rule, section 17 docs spec, Notes "Variable scope"), step files (2 types, 3 loader + tests, 7 glob-matcher matcher rejection, 9 template-expansion adds `findGroupDir` + extends `expandPerFile` with `groupDir` opt + extends `hasPerFileVariable` + new `hasGroupDirVariable`, 10 prepare-commands extends grouping rule + tests), CONFIGURATION.md (new schema row, new variable row, new "Grouping per package" subsection, fixed example 3, extended grouping rules + shell-quoting list).

- **Audit log added to the plan after step 1 completed.** A new step (15-audit-log) was inserted between the original step 14 (smoke tests) and step 15 (update documentation), pushing update-documentation to step 16. The audit log is a structured, always-on record of every Stop event under `<project>/.claude/tools-runner-log/YYYY-MM/DD/HH.{json,log}`. The audit-log feature requires NEW user-facing content in CONFIGURATION.md (a new "Audit log" section), HOW_IT_WORKS.md (a brief subsection listing entry types), and DEVELOPMENT.md (one-liner inspection commands). Step 1's docs do NOT yet contain this content: it is added by step 15 when the audit-log feature is implemented, and step 16's review pass verifies it. References to the dropped env vars and the file-size cap have already been cleaned out of step 1's artifact during this revision pass.

- **Multi-repo discovery + per-file `${{repo}}`.** The original model assumed a single repo: `findRepoRoot(projectDir)` returned at most one repo, and `${{repo}}` was a project-wide value bound at the start of the hook invocation. The user clarified that `${{repo}}` should be the repo containing the *matched file*, which may be a parent of `${{project}}`, a child of it, or `${{project}}` itself; multiple repos can be active at once, and the discovery walk has to go both UP (for an enclosing repo) and DOWN (for nested repos). Resolution: `findRepoRoot` was replaced by `discoverRepos(projectDir): Promise<DiscoveredRepo[] | GitMissing>` which performs both walks and unions the results; `ChangedFile` gained a `repoRoot` field; `${{repo}}` moved from `TemplateContext` to a per-file substitution sourced from `file.repoRoot`; variables in `paths` were dropped entirely (pre-match anchoring across multiple repos doesn't work, and `expandPathPattern` was removed); the grouping rule in `compileCommands` gained a per-repo tier (between per-group and per-trigger) for templates that reference `${{repo}}` without finer variables; the file-hash cache key changed from bare `file.path` to `${path.basename(file.repoRoot)}:${file.path}` (e.g. `tools-runner:src/foo.ts`) to disambiguate repos while staying YAML-readable, with a `repoRoot` field added to `FileHashEntry` so two repos sharing a basename can be unambiguously distinguished at lookup time (read-side verifies `entry.repoRoot === file.repoRoot` and treats a mismatch as a miss); a new `fileHashKey(file)` helper exposes the canonical key shape and `CommandRunEntry.matchedFiles` was reshaped from `string[]` to `MatchedFileRef[]` (`{ repoRoot, path }`) so prune-time can re-derive the cache key for each surviving reference; the audit log gained a new `repos_discovered` entry type and the `hook_started` entry now records `projectDir`; the `[tools-runner] not inside a git repo, skipping` log line was replaced with `[tools-runner] no git repos found, skipping` triggered by `discoverRepos` returning an empty array. Propagated through main plan (Hook behavior table, Log line catalog, sections 7/8/9/10/11/14), step files (6 git-collector full rewrite for `discoverRepos`, 7 glob-matcher drops `ctx` and variable expansion, 8 file-hashing adds `fileHashKey` and repo-prefixed cache keys, 9 template-expansion drops `expandPathPattern` and adds `hasRepoVariable` + `${{repo}}` per-file in `expandPerFile`, 10 prepare-commands adds per-repo tier, 13 stop-hook calls `discoverRepos` and unions per-repo `collectChangedFiles` results, 15 audit-log adds `repos_discovered` and reshapes `changed_files`), CONFIGURATION.md (`${{repo}}` row marked per-file, "Variables aren't supported in `paths`" note, multi-repo subsection with worked examples), HOW_IT_WORKS.md (architecture diagram updated, new "Repo discovery" section, `state.fileHashes` keying scheme updated). The previous deviation note in this Summary that said variables-in-paths were supported has been superseded by this entry: they are no longer supported.

- **Per-repo configuration files.** The user proposed extending the layered-config model so each discovered repo can have its own `<repoRoot>/.claude/tools-runner.yaml`, scoped to that repo. Resolution: the registry now accepts a variable number of layers (home + project + one per discovered repo other than the project itself). Each layer carries a `LayerScope` (`"home" | "project" | "repo"`), its own `TemplateContext` (`{ projectDir }` for home/project, `{ projectDir: repoRoot }` for per-repo so `${{project}}` is rebound to the repo root), and an optional `repoFilter` (the repo's root for per-repo layers, null otherwise). `compileCommands` applies `repoFilter` before glob-matching so per-repo triggers can only match files from their own repo, and uses the per-entry `ctx` for variable expansion (the standalone `ctx` argument was dropped: context is per-layer, not per-invocation). Skip rule: when `repo.root === projectDir`, the per-repo layer is not loaded: the project layer already covers that file. The audit log's `config_load` entry gained `scope` and (for `scope === "repo"`) `repoRoot` fields; the number of `config_load` entries per invocation is now `2 + N` (home + project + N per-repo layers). New helpers `repoConfigPath(repoRoot)` and `repoDisplayPath(repoRoot)` were added to `src/config.ts`. The Hook behavior table's "Neither home nor project YAML exists" row was generalised to "Every layer empty"; the per-layer parse-error rows were collapsed into a single "Any one YAML has a parse or validation error → treat that one as empty, continue with the others" row. Inside a per-repo config, `${{project}}` and `${{repo}}` are interchangeable (both equal the layer's repo root); the per-repo grouping tier in `compileCommands` collapses to a single emit because the `repoFilter` ensures all matched files share the layer's repo root. Auto-reload via `fs.watchFile` works identically to home/project layers. Propagated through main plan (Overview, Hook behavior table, Log line catalog, sections 4/5/11/14/17, Notes "Layered config" and "Variable scope"), step files (3 config-loader adds `repoConfigPath`/`repoDisplayPath` + tests, 4 trigger-registry full update for variable layer count + `LayerScope`/`ctx`/`repoFilter` on each layer, 6 git-collector adds a sentence pointing forward to step 13's per-repo config-load, 10 prepare-commands threads per-entry `ctx` and applies `repoFilter`, 13 stop-hook builds variable-length registry with the project-IS-repo skip rule, 15 audit-log extends `config_load` with `scope` + `repoRoot`), CONFIGURATION.md (new "Per-repo configs" subsection, "Configuration files" lists three locations, variable table updated for per-repo override of `${{project}}`/`${{repo}}`), HOW_IT_WORKS.md (architecture diagram now shows discover-repos before load-layers and includes per-repo `FileLayer`s; "Layered config loading" section describes per-layer ctx + repoFilter; "Repo discovery" section calls out that discovered repos drive per-repo config loading; "CompiledCommand grouping" section calls out the `repoFilter` filter step).

- **All Bun-specific stdlib APIs banned (not just `Bun.spawn`).** Earlier revisions still used `Bun.file(filePath).text()` for YAML/state reads, `Bun.file(absPath).bytes()` for hash-input bytes, and `Bun.write(filePath, content)` for atomic state and test-fixture writes. The user directed that the source must avoid all Bun-specific functions: the plugin runs on Bun but should call only Node-compatible stdlib (`node:fs/promises`) so the source stays portable. Resolution: every `Bun.file(...).text()` becomes `fs.readFile(path, "utf8")`; every `Bun.file(...).bytes()` becomes `fs.readFile(path)` (returns a `Buffer`/`Uint8Array` directly); every `Bun.write(path, content)` becomes `fs.writeFile(path, content)`; the manifest-JSON verification one-liner in step 2 was rewritten to use `fs.readFile` instead of `Bun.file`. Propagated through main plan (Implementation conventions section's banned-list, sections 4.2/6.1/9.1), step files (2 project-skeleton verification command, 3 config-loader load + IO conventions + test fixtures, 5 state-file load/save + IO conventions, 6 git-collector test fixture wording, 8 file-hashing miss-path read + IO conventions + test fixtures, 11 cooldown-gate test fixtures, 13 stop-hook test fixtures, 4 trigger-registry IO-convention sentence), and HOW_IT_WORKS.md (the hash-computation step now reads bytes via `fs.readFile`). The Implementation conventions section now bans every `Bun.*` stdlib helper, not just spawn; the bundler note about why a Node-side syntax check is skipped drops the `Bun.*` example since source code no longer emits any (the surviving reason is `import.meta.main`).

Nothing deferred. The "Verification" section of the step file calls for running existing tests; no source code is added in this step, so the unit/smoke suites do not yet exist. `/verify` was therefore not run for this step: it is the gate for steps that produce code.
