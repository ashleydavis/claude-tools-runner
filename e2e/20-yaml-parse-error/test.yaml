description: "malformed YAML in project config exits 2"

setup:
  git_init:
    - "."
  files:
    .claude/claude-tools-runner.yaml: |
      triggers: [ { paths: ["**/*.ts"], commands: [

steps:
  - description: "hook exits 2 with the catalog stderr message so Claude Code feeds the parse error back to the model"
    expect:
      exit: 2
      stderr_substrings:
        - "[tools-runner] failed to load .claude/claude-tools-runner.yaml:"
