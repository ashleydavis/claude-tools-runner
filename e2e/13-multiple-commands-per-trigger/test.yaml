description: "multiple commands per trigger fan out as separate invocations"

setup:
  git_init:
    - "."
  files:
    .claude/claude-tools-runner.yaml: |
      triggers:
        - paths:
            - "**/*.ts"
          commands:
            - run: "echo A > a.out"
            - run: "echo B > b.out"
    foo.ts: ""

steps:
  - description: "both commands run; both outputs land on disk"
    expect:
      exit: 0
      file_contains:
        - path: a.out
          substring: A
        - path: b.out
          substring: B
